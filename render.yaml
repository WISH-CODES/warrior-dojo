services:
  - type: web
    name: warrior-dojo
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /docs  # FastAPI docs endpoint for health checks